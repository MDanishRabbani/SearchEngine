function search() {
  const query = document.getElementById("query").value;
  const topk = document.getElementById("topk").value;
  const engine = document.getElementById("engine").value;

  // Perform AJAX request to the server-side script
  // Send query, topk, and engine to the back-end
  // Receive and display the results in the 'results' div
}
